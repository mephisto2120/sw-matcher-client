import React from 'react';
import {mount} from 'enzyme';
import PersonSearchingView from 'PersonSearchingView';
import Root from 'Root';

it('should return searched person', () => {
  // const component = mount(
  //   <Root><PersonSearchingView/></Root>
  // );
  //
  // const firstNameDivText = component.find({id: "firstName"}).text();
  //
  // expect(firstNameDivText).toEqual('First name');
  // component.unmount();
});
